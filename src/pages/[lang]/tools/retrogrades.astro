---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import RetrogradesApp from '../../../components/chart/RetrogradesApp.tsx';
import { getTranslations, supportedLocales, type Locale } from '../../../i18n';
export function getStaticPaths() { return supportedLocales.map(lang => ({ params: { lang } })); }
const { lang } = Astro.params;
const locale = lang as Locale;
const t = getTranslations(locale);
---
<BaseLayout title={t.nav.retrograde} locale={locale}>
  <div class="max-w-5xl mx-auto px-4 sm:px-6 py-8">
    <div class="mb-6">
      <h1 class="text-2xl font-serif font-bold text-cream">{t.nav.retrograde}</h1>
    </div>
    <RetrogradesApp client:load locale={locale} />
  </div>
</BaseLayout>
