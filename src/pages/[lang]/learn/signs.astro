---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getTranslations, supportedLocales, localePath, type Locale } from '../../../i18n';
import { getContent } from '../../../content';

export function getStaticPaths() {
  return supportedLocales.map(lang => ({ params: { lang } }));
}

const { lang } = Astro.params;
const locale = lang as Locale;
const t = getTranslations(locale);
const content = await getContent(locale, 'signs');
---

<BaseLayout title={content.title} locale={locale}>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 py-8">
    <nav class="text-sm text-muted mb-6">
      <a href={localePath('/learn', locale)} class="hover:text-brand-600">{t.nav.learn}</a> → <span class="text-cream">{content.title}</span>
    </nav>
    <h1 class="text-3xl font-serif font-bold text-cream mb-4">{content.title}</h1>
    <p class="text-muted mb-8 text-lg">{content.intro}</p>

    <div class="space-y-6">
      {content.list.map((sign: any) => (
        <article class="bg-base-50 rounded-xl border border-base-300 p-6 shadow-sm">
          <div class="flex items-start gap-4">
            <div class="text-4xl flex-shrink-0" style={`color: ${sign.color}`}>{sign.symbol}</div>
            <div class="flex-1">
              <h2 class="text-xl font-serif font-bold text-cream">{sign.name}</h2>
              <div class="flex flex-wrap gap-3 mt-1 text-xs text-muted">
                <span>{sign.dates}</span>
                <span style={`color: ${sign.color}`}>● {sign.element}</span>
                <span>{sign.mode}</span>
                <span>{t.common.ruler}: {sign.ruler}</span>
              </div>
              <p class="mt-3 text-sm text-cream-dark leading-relaxed">{sign.description}</p>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</BaseLayout>
